# discord-forward-auth
An independent, reusable Traefik ForwardAuth authentication service that enables any Traefik-protected application to use Discord for "human user login + guild/role authorization" without requiring any changes to the protected application itself.
